bin/
obj/
*.user
*.suo
.vs/

# Isolated feature worktrees
.worktrees/

# Test and publish output
TestResults/
coverage/

# Launcher publish output — rebuilt from source, never committed
tools/SentryLauncher/dist/
artifacts/
AppPackages/
BundleArtifacts/
*.nupkg
*.snupkg
*.msix
*.msixbundle
*.appx
*.appxbundle

# Local secrets and signing material
.env
.env.*
!.env.example
appsettings.Local.json
appsettings.*.local.json
local.settings.json
*.pfx
*.p12
*.pem
# Cloudflare Tunnel credentials. The tunnel's ingress config IS committed
# (deploy/linux/cloudflared/config.yml); its credentials never are.
deploy/linux/cloudflared/creds.json
deploy/linux/cloudflared/*.json
*.key

# Voice recordings and generated audio
recordings/
*.wav
*.mp3
*.m4a
*.ogg

# Sentry Gateway local state
server/sentry_gateway/.venv/
deploy/linux/data/
__pycache__/
*.pyc
.pytest_cache/

# Node host local configuration (names local paths)
appsettings.node.json

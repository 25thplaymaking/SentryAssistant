<?xml version="1.0" encoding="utf-8"?>
<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Style x:Key="SentryEyebrowTextStyle" TargetType="TextBlock">
        <Setter Property="FontFamily" Value="Cascadia Mono" />
        <Setter Property="FontSize" Value="11" />
        <Setter Property="FontWeight" Value="SemiBold" />
        <Setter Property="CharacterSpacing" Value="140" />
        <Setter Property="Foreground" Value="{ThemeResource SentryTextSecondaryBrush}" />
    </Style>

    <Style x:Key="SentryMonoMetaTextStyle" TargetType="TextBlock">
        <Setter Property="FontFamily" Value="Cascadia Mono" />
        <Setter Property="FontSize" Value="10" />
        <Setter Property="CharacterSpacing" Value="90" />
        <Setter Property="Foreground" Value="{ThemeResource SentryTextMutedBrush}" />
    </Style>

    <Style x:Key="SentryPageTitleStyle" TargetType="TextBlock">
        <Setter Property="FontFamily" Value="Segoe UI Variable Display" />
        <Setter Property="FontSize" Value="24" />
        <Setter Property="FontWeight" Value="SemiBold" />
        <Setter Property="Foreground" Value="{ThemeResource SentryTextPrimaryBrush}" />
    </Style>

    <Style x:Key="SentryNavButtonStyle" TargetType="ToggleButton">
        <Setter Property="Height" Value="36" />
        <Setter Property="Padding" Value="10,0" />
        <Setter Property="HorizontalContentAlignment" Value="Stretch" />
        <Setter Property="HorizontalAlignment" Value="Stretch" />
        <Setter Property="Foreground" Value="{ThemeResource SentryTextSecondaryBrush}" />
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="CornerRadius" Value="{StaticResource SentryControlRadius}" />
        <Setter Property="UseSystemFocusVisuals" Value="True" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ToggleButton">
                    <Grid x:Name="Root" Padding="{TemplateBinding Padding}"
                          Background="{TemplateBinding Background}" CornerRadius="{TemplateBinding CornerRadius}">
                        <VisualStateManager.VisualStateGroups>
                            <VisualStateGroup x:Name="CommonStates">
                                <VisualState x:Name="Normal" />
                                <VisualState x:Name="PointerOver">
                                    <Storyboard>
                                        <ObjectAnimationUsingKeyFrames Storyboard.TargetName="Root" Storyboard.TargetProperty="Background">
                                            <DiscreteObjectKeyFrame KeyTime="0" Value="{ThemeResource SentrySurfaceHoverBrush}" />
                                        </ObjectAnimationUsingKeyFrames>
                                    </Storyboard>
                                </VisualState>
                                <VisualState x:Name="Pressed">
                                    <Storyboard>
                                        <ObjectAnimationUsingKeyFrames Storyboard.TargetName="Root" Storyboard.TargetProperty="Background">
                                            <DiscreteObjectKeyFrame KeyTime="0" Value="{ThemeResource SentryNavSelectedBrush}" />
                                        </ObjectAnimationUsingKeyFrames>
                                    </Storyboard>
                                </VisualState>
                                <VisualState x:Name="Disabled">
                                    <Storyboard><DoubleAnimation Storyboard.TargetName="Root" Storyboard.TargetProperty="Opacity" To="0.45" Duration="0" /></Storyboard>
                                </VisualState>
                            </VisualStateGroup>
                            <VisualStateGroup x:Name="CheckStates">
                                <VisualState x:Name="Unchecked">
                                    <Storyboard><DoubleAnimation Storyboard.TargetName="SelectionBar" Storyboard.TargetProperty="Opacity" To="0" Duration="0" /></Storyboard>
                                </VisualState>
                                <VisualState x:Name="Checked">
                                    <Storyboard>
                                        <ObjectAnimationUsingKeyFrames Storyboard.TargetName="Root" Storyboard.TargetProperty="Background">
                                            <DiscreteObjectKeyFrame KeyTime="0" Value="{ThemeResource SentryNavSelectedBrush}" />
                                        </ObjectAnimationUsingKeyFrames>
                                        <DoubleAnimation Storyboard.TargetName="SelectionBar" Storyboard.TargetProperty="Opacity" To="1" Duration="0:0:0.12" />
                                    </Storyboard>
                                </VisualState>
                                <VisualState x:Name="Indeterminate" />
                            </VisualStateGroup>
                        </VisualStateManager.VisualStateGroups>
                        <Border x:Name="SelectionBar" Width="2" Height="16" Margin="0,0,7,0" HorizontalAlignment="Left"
                                CornerRadius="1" Background="{ThemeResource SentryAccentBrush}" Opacity="0" />
                        <ContentPresenter Margin="7,0,0,0" HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                          VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                          Content="{TemplateBinding Content}" ContentTemplate="{TemplateBinding ContentTemplate}" />
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="SentrySurfaceButtonStyle" TargetType="Button">
        <Setter Property="CornerRadius" Value="{StaticResource SentryControlRadius}" />
        <Setter Property="Padding" Value="12,7" />
        <Setter Property="Background" Value="{ThemeResource SentrySurfaceElevatedBrush}" />
        <Setter Property="BorderBrush" Value="{ThemeResource SentryBorderBrush}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="Foreground" Value="{ThemeResource SentryTextPrimaryBrush}" />
    </Style>

    <Style x:Key="SentryPrimaryButtonStyle" TargetType="Button" BasedOn="{StaticResource SentrySurfaceButtonStyle}">
        <Setter Property="Background" Value="{ThemeResource SentryAccentMutedBrush}" />
        <Setter Property="BorderBrush" Value="{ThemeResource SentryAccentBrush}" />
        <Setter Property="Foreground" Value="{ThemeResource SentryTextPrimaryBrush}" />
        <Setter Property="FontWeight" Value="SemiBold" />
    </Style>

    <Style x:Key="SentryIconButtonStyle" TargetType="Button" BasedOn="{StaticResource SentrySurfaceButtonStyle}">
        <Setter Property="Width" Value="36" />
        <Setter Property="Height" Value="36" />
        <Setter Property="Padding" Value="0" />
    </Style>

    <Style x:Key="SentryTextBoxStyle" TargetType="TextBox">
        <Setter Property="CornerRadius" Value="{StaticResource SentryControlRadius}" />
        <Setter Property="Background" Value="{ThemeResource SentrySurfaceElevatedBrush}" />
        <Setter Property="BorderBrush" Value="{ThemeResource SentryBorderStrongBrush}" />
        <Setter Property="Foreground" Value="{ThemeResource SentryTextPrimaryBrush}" />
    </Style>
</ResourceDictionary>
